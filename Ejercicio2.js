function op(arreg) {
    let suma=0, prom=0;
    for(let i;i<arreg;i++){
        suma=suma+arreg[i];
    }
    prom=suma/length.arreg;
    console.log("La suma es ",suma +" y el promedio es ",prom);
}